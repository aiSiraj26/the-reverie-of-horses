{
  "name": "the-reverie-of-horses",
  "private": true,
  "version": "0.0.0",
  "description": "Tests for The Reverie of Horses static site.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "serve": "npx --yes http-server -p 4173 -c-1 -s ."
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
